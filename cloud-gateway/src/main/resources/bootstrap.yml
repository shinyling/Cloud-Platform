spring:
  application:
    name: gateway-api
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
server:
  port: 8888